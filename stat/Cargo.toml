[package]
name    = "stat"
version = "1.0.0"
edition = "2021"

[dependencies]
colored     = "2"
windows-sys = { version = "0.52", features = ["Win32_Storage_FileSystem", "Win32_Foundation"] }

[profile.release]
opt-level     = 3
lto           = true
strip         = true
codegen-units = 1
